# .env is better suited for public variables, ie, variables that should not be committed
# For secret variables use .env.local (which is gitignored)
# Or use DevServerControl tool with set_env_variable: ["KEY", "SECRET"]

# https://www.builder.io/c/docs/using-your-api-key
# Replace with your actual key or remove if not using Builder.io
VITE_PUBLIC_BUILDER_KEY=

# Custom environment variables
PING_MESSAGE="ping pong"
